namespace dou2d {
    /**
     * 资源类型
     * @author wizardc
     */
    export const enum AssetType {
        text = "text",
        json = "json",
        binary = "binary",
        image = "image",
        sheet = "sheet",
        sound = "sound"
    }
}
