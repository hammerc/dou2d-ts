namespace dou2d {
    /**
     * 颜色填充类型
     * @author wizardc
     */
    export const enum GradientType {
        /**
         * 用于指定线性渐变填充的值
         */
        linear = "linear",
        /**
         * 用于指定放射状渐变填充的值
         */
        radial = "radial"
    }
}
