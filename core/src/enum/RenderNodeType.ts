namespace dou2d.rendering {
    /**
     * 渲染节点类型
     * @author wizardc
     */
    export const enum RenderNodeType {
        normalBitmapNode = 1,
        bitmapNode,
        textNode,
        graphicsNode,
        groupNode,
    }
}
